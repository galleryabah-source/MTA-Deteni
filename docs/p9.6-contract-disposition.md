# P9.6 Database Adapter & Contract Reconciliation — Contract Disposition

## Baseline

- Verification commit: `24760d25cce15d8793f4a020b6baa36a14b0cdc0`
- Domain CI #1930: SUCCESS
- Migration freeze: ACTIVE
- Schema changes: NONE
- Production migration replay: PROHIBITED during P9.6

## Disposition

### Outbox lease recovery
Status: DEFERRED.

Production `mta_internal.claim_outbox_events(p_limit, p_lease_seconds)` accepts a lease parameter, but the current function definition does not use `p_lease_seconds` to recover stale `PROCESSING` events. This remains a post-freeze hardening item. No database mutation is performed in P9.6.

### Production migration lineage
Status: OBSERVED BASELINE.

Production migration history contains 22 records. The repository currently contains a smaller set of migration files and therefore must not be treated as a replayable representation of production history. P9.6 records the production lineage as observed baseline; it does not recreate or replay missing migrations.

### Database abstraction
Status: CANONICAL CONTRACT IDENTIFIED; LEGACY CONTRACT DEPRECATION CANDIDATE.

`src/application/database-adapter-contract.ts` is the canonical database adapter contract for the current architecture. `src/application/database-contract.ts` remains a separate legacy abstraction with a different API. It is not deleted in P9.6 because removal requires a complete dependency/reference proof and is not necessary for production bridge operation.

### Production bridge
Status: VERIFIED.

The production API path uses the Supabase Edge Function bridge and PostgreSQL RPCs for durable idempotency, audit, and outbox behavior. Direct PostgreSQL connectivity from the Cloudflare Worker is not introduced.

## Governance

P9.6 does not authorize schema mutation, migration execution, or production database changes. Any deferred database hardening requires a separate approved change after the migration-freeze gate is lifted.

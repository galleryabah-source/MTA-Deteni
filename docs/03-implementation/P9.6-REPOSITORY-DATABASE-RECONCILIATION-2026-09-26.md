# MTA DETENI — P9.6 Repository / Database Reconciliation — 2026-09-26

## Gate
P9.6 — IN PROGRESS / NOT CERTIFIED

Governance locks: MIGRATION_FREEZE=TRUE, AI=OFF, SYNTHETIC DATA ONLY, PRODUCTION ACCESS=NOT AUTHORIZED, LIVE POSTGRESQL EXECUTION=BLOCKED.

## Current controlled finding
The previous local replay reached the first auth/RBAC helper migration and failed because `public.mta_current_role()` was assumed to exist before the later repository migration that creates/replaces it. This is treated as a repository baseline dependency, not as permission to modify production migrations.

The controlled replay now models that pre-existing function only inside the disposable local compatibility layer, then replays the repository migrations unchanged. The resulting schema is introspected read-only and compared against repository migration evidence.

## Certification boundary
This gate does not access or certify the production database, does not execute production migrations, and does not enable AI.

## Remaining certification work
1. Controlled local replay PASS.
2. Read-only PostgreSQL introspection PASS.
3. Expected-vs-actual reconciliation matrix generated.
4. DB-001 through DB-020 PASS.
5. Resolve all P0/P1 reconciliation findings before any migration proposal.
6. Generate P9.6 certification evidence.

Status: NOT CERTIFIED until all gates above are evidenced.

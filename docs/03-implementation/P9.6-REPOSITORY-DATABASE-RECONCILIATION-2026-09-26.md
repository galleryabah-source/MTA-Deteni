# MTA DETENI — P9.6 Repository / Database Reconciliation — 2026-09-26

## Gate

P9.6 — IN PROGRESS / NOT CERTIFIED

Governance locks: MIGRATION_FREEZE=TRUE, AI=OFF, SYNTHETIC DATA ONLY, PRODUCTION ACCESS=NOT AUTHORIZED, LIVE POSTGRESQL EXECUTION=BLOCKED.

## Audit basis

Repository baseline: commit 332d5071ec4acb922988894a9c9bb77b72cd353c.

Evidence includes the P9.6 contract, application database adapter contract, PostgreSQL boundary, transaction/repository contracts, package.json, and Supabase migration files.

## Implementation finding

A database boundary already exists, but the repository is not live-bound to PostgreSQL. The application database adapter factory intentionally returns DATABASE_ADAPTER_RUNTIME_NOT_BOUND. The persistence layer contains transaction and repository contracts, but no concrete PostgreSQL pool/ORM implementation. package.json contains no PostgreSQL driver or ORM dependency.

This gate adds a provider-neutral driver-injection adapter. It exercises the approved boundary without coupling application/domain code to a PostgreSQL library and without creating a live connection.

## Repository schema evidence

The current migration set contains concrete objects for: mta_detainees, mta_placements, mta_movements, mta_leaves, mta_documents, mta_audit_events, mta_storage_objects, mta_ai_jobs, mta_profiles, the private storage bucket, and audit hash-chain functions/triggers.

The P9.6 expected contract is broader. Current migration evidence does not establish dedicated objects for rooms, beds, room_barcode_tokens, leave approvals, leave QR tokens, leave handovers, escort officers/checkpoints/vehicles, duty rosters, follow-up requests/recipients, emergency incidents, overdue/reconciliation/exception structures, notification/event/delivery/template/policy/acknowledgement/failure/webhook structures, intake/extraction/validation/review staging, and reporting/analytics/management/export/certification structures.

These are not automatically declared absent from the live database. Their live state is UNKNOWN until controlled non-production introspection is performed.

## Live database evidence

No live PostgreSQL introspection was performed in this gate.

Therefore live tables, columns, indexes, constraints, RLS policies, database roles, and applied migration state remain UNKNOWN. Comments inside migration files claiming application to a Supabase project are repository assertions, not live verification evidence.

## Blocking findings

P9.6-F01 — P1: PostgreSQL driver/ORM is not installed. Driver strategy must be explicitly selected and isolated.

P9.6-F02 — P1: live schema cannot be certified from migration files alone. Controlled non-production introspection is required.

P9.6-F03 — P1: expected contract exceeds current migration evidence. Each missing domain must be reconciled before schema changes are proposed.

P9.6-F04 — P1: application adapter contract and P9.6 canonical document differ in naming/lifecycle shape. This must be normalized without creating a second competing database identity model.

## Evidence added by this gate

- src/infrastructure/database/p9.6-database-adapter.ts
- test/p9.6-database-adapter.test.ts
- this reconciliation document

## Certification criteria remaining

1. Select approved PostgreSQL driver strategy.
2. Introspect an explicitly approved controlled non-production PostgreSQL target.
3. Complete expected-vs-actual schema matrix, including tables, columns, indexes, constraints, RLS policies and roles.
4. Execute DB-001 through DB-020.
5. Resolve all P0/P1 database findings.
6. Generate P9.6 certification evidence.
7. Do not execute migration during this audit gate.

Status: NOT CERTIFIED.

# MTA DETENI — P9.6 Repository / Database Reconciliation — 2026-09-26\n\n## Gate\nP9.6 — IN PROGRESS / NOT CERTIFIED\n\nGovernance locks: MIGRATION_FREEZE=TRUE, AI=OFF, SYNTHETIC DATA ONLY, PRODUCTION ACCESS=NOT AUTHORIZED, LIVE POSTGRESQL EXECUTION=BLOCKED.\n\n## P9.6-R1 — Effective-State Reconciliation Engine\n\nThe first controlled artifact exposed false-positive reconciliation results caused by name-only parsing:\n\n- PostgreSQL indexes generated by primary-key/unique constraints were reported as EXTRA.\n- Historical policies that were intentionally replaced by later DROP POLICY + CREATE POLICY migrations were reported as MISSING.\n\nP9.6-R1 corrects the reconciliation engine without changing schema or production state.\n\n### R1 controls\n\n1. Policies are reconciled from the effective final migration state, applying sequential DROP POLICY and CREATE POLICY lifecycle events.\n2. PostgreSQL indexes backed by PRIMARY KEY or UNIQUE constraints are recognized as justified database-generated indexes rather than unexplained EXTRA objects.\n3. Genuine unexplained indexes remain EXTRA.\n4. Reconciliation summaries now reserve explicit counters for CONFLICT, UNSAFE, and UNKNOWN.\n5. The matrix records that definition-level semantic reconciliation is outside the current object-name-only comparison scope; these statuses are not manufactured without sufficient evidence.\n6. Regression tests cover policy replacement, constraint-backed indexes, and genuine unexplained indexes.\n7. CI executes the reconciliation-engine regression suite before local migration replay.\n\n## Safety boundary\n\nP9.6-R1 does not:\n\n- access production PostgreSQL;\n- execute production migrations;\n- alter Supabase schema;\n- enable AI;\n- change runtime canonical data;\n- remove or modify existing production indexes/policies.\n\nAll validation remains against disposable local PostgreSQL.\n\n## Certification boundary\n\nP9.6 remains NOT CERTIFIED until the controlled artifact demonstrates:\n\n1. local replay PASS;\n2. read-only introspection PASS;\n3. effective-state reconciliation matrix generated;\n4. no unexplained P0/P1 reconciliation findings;\n5. DB-001 through DB-020 PASS;\n6. final certification evidence generated.\n\nThe R1 engine correction is therefore a prerequisite for, not a substitute for, the final P9.6 certification gate.\n
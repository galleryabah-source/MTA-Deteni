# MTA DETENI — P9.13 Execution Status

## Current decision

`NOT_CERTIFIED` — the executable kernel slice is implemented and locally verified, but repository CI has not produced a usable evidence record.

## Implemented executable slice

- fail-closed configuration loader;
- AI-OFF operation without AI credentials;
- deny-by-default authorization;
- permission + scope + operational assignment + duty + classification + resource/state/policy checks;
- explicit Super Admin operational bypass denial;
- canonical audit hash chain writer/verifier;
- PostgreSQL adapter boundary with transactional rollback wrapper;
- dependency-free Node test harness;
- GitHub Actions workflow.

## Local verification

Harness: `node --test tests/p9-kernel.test.mjs`

Result: **7/7 PASS**

Covered:

1. AI-OFF core availability;
2. production configuration fail-closed;
3. valid authorization;
4. scope denial;
5. inactive duty denial;
6. Super Admin operational bypass denial;
7. audit tamper detection.

## CI status

GitHub Actions runs were triggered for the implementation branch, but the connector exposes no step-level logs and reports the job as `failure`. The failure therefore cannot be attributed to the implementation with sufficient evidence.

The workflow was changed to use only the Node built-in test runner and no package-registry dependency. The latest run still does not expose a usable step-level evidence record.

## Certification rule

This status does **not** mark P9.13 as certified.

Required before certification:

- usable CI evidence or equivalent controlled execution evidence;
- complete K1–K12 verification;
- transaction/idempotency and outbox executable tests;
- private-storage verification;
- observability and request/correlation propagation verification;
- production-database guard verification;
- security negative certification;
- AI-OFF verification across all critical paths.

## Safety boundary

`MIGRATION_FREEZE = TRUE`

No production database, real detainee data, credentials, or operational secrets were used.

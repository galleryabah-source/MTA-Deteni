# P10.29 — Document API Boundary Contract

Status: PREPARED — synthetic contract only.

Document operations use a server-side boundary: authentication → authorization (document permission + scope + duty/policy) → state check → immutable template/version/hash binding → deterministic DOCX generation → artifact identity/hash → lifecycle → audit → outbox where required.

Required outputs remain `TEMPORARY_EXIT_PERMISSION` (Surat Izin Keluar Sementara) and `ESCORT_ASSIGNMENT_LETTER` (Surat Tugas Pengawalan).

Clients cannot select arbitrary template paths. Download requires a persistent grant bound to document/artifact/hash/object/actor/scope/expiry and atomic single-use consumption. Provider/network calls are post-commit only.

No real detainee data, document contents, credentials, tokens or secrets enter synthetic evidence.

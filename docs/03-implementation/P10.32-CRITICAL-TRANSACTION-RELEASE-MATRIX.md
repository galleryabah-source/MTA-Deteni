# P10.32 — Critical Transaction Release Matrix

Status: PREPARED — synthetic contract.

| Operation | AuthZ | Transaction | Audit | Outbox | Idempotency |
|---|---|---|---|---|---|
| Leave critical transition | yes | yes | mandatory | downstream | yes |
| Document issue | yes | yes | mandatory | downstream | yes |
| Artifact grant creation | yes | yes | mandatory | yes | yes |
| Artifact consume | yes | atomic transition | mandatory | no provider in tx | atomic |
| RBAC mutation | yes | yes | mandatory | cache event | optimistic version |

Domain failure, mandatory audit failure, outbox failure or commit failure fails safely. Provider failure after commit is handled by retry/dead-letter; it never retroactively mutates committed domain state.

No migration or production execution is authorized.

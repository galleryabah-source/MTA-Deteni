# P10.18 — Document Output Operationalization Package

PREPARED — synthetic/static only. `MIGRATION_FREEZE=TRUE`.

Mandatory Word outputs: `TEMPORARY_EXIT_PERMISSION` (Surat Izin Keluar Sementara) and `ESCORT_ASSIGNMENT_LETTER` (Surat Tugas Pengawalan), both `.docx`.

Templates use immutable template ID/version, renderer version and SHA-256. Field sources are limited to authorization, document and system data; arbitrary client fields are rejected.

Lifecycle: `DRAFT → PENDING_APPROVAL → APPROVED → ISSUED → ARCHIVED`; `VOID` is terminal. Roles are PREPARER, APPROVER, ISSUER and ARCHIVER with separation of duties.

Issued artifacts bind document, authorization, template/version, artifact ID/SHA-256, scope and correlation identifiers. Download uses a single-use grant bound to actor, scope, object, artifact and expiry. Replay, mismatch, expiry and revoke fail closed; concurrent consume must produce exactly one successful transition.

Grant, audit and outbox share one transaction client. Provider/network calls occur only after commit. AI is OFF and is not required for document generation or issuance.

Synthetic evidence only; no detainee PII, health information, production identifiers, document contents, credentials, tokens or provider secrets. `SKIPPED_BY_SAFETY_GATE` is never PASS.

Next checkpoint: P10.19 — Document Output Contract Test & Synthetic Fixture Hardening.

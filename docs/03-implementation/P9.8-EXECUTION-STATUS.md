# MTA DETENI — P9.8 Executable Implementation Status

**Status:** IMPLEMENTED / EXECUTION EVIDENCE PENDING

## Scope

The branch contains a dependency-free executable outbox/idempotency foundation plus a transactional command-boundary test kernel aligned to the P9.8 contract. It remains an in-memory kernel test double until the approved PostgreSQL schema and adapter integration are available.

## Implemented invariants

- immutable event identity via `eventId`;
- event contract validation;
- correlation metadata requirement;
- `PENDING → PROCESSING → SUCCEEDED` lifecycle;
- retry lifecycle;
- dead-letter lifecycle;
- lease owner and lease expiry;
- stale-lease recovery;
- duplicate event identity rejection;
- deterministic request fingerprinting;
- same-key replay;
- same-key/different-payload conflict;
- worker lease ownership on completion/retry/dead-letter;
- AI/provider independence at kernel level;
- atomic command model: domain mutation + audit + outbox;
- rollback on domain, audit, outbox, or commit failure in the test kernel;
- no external-provider call is modeled inside the critical transaction.

## Executable evidence

Local Node built-in test execution passed **12/12** tests across the transactional-boundary and private-storage slices used for this checkpoint. The P9.8 transaction-boundary suite specifically covers atomic commit, rollback on outbox/audit failure, idempotent replay, key-reuse conflict, and provider-call isolation.

## Deliberate boundary

This implementation does **not** claim transactional database atomicity. That requires integration with the approved database adapter and schema. No migration is performed here.

GitHub Actions is configured to execute the kernel suite, but the connector currently exposes workflow failure status without actionable runner step logs. Therefore CI is not represented as PASS until independently observable evidence is available.

## Gate

P9.8 remains `NOT_CERTIFIED` until PostgreSQL integration, failure-injection, and concurrency evidence satisfies the contract's required test matrix.

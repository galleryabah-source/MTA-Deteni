# P12.681–840 — Operational Evidence and Cross-Domain Integrity Gate

## Completed boundaries

### P12.681–720 — Operational Evidence Ledger
Audit events are normalized into operational evidence while preserving event, aggregate, actor, correlation and payload-hash identity. The ledger is append-only at the contract boundary.

### P12.721–760 — Projection Failure / Recovery
Projection failures are classified deterministically:
- TRANSIENT → RETRY
- PERMANENT → QUARANTINE
- IDENTITY_DRIFT → HALT
- CHECKPOINT_DRIFT → REBUILD_FROM_EVIDENCE

### P12.761–800 — API Audit Envelope
Accepted commands require an audit event identity. Rejected/failed commands require an explicit error code. Request, command, actor and correlation identity remain bound.

### P12.801–840 — Cross-Domain Consistency
RAP, PERKES, KAMTIB, SUBBAG TU and LEADERSHIP records participating in one operational correlation must preserve detainee, aggregate and correlation identity. Mixed identities are blocked.

## Integrated invariant

`one operational action → one identity chain → one authoritative evidence trail → derived projections`

## Safety

Migration Freeze TRUE; AI OFF; synthetic-only repository; production access NOT AUTHORIZED; live PostgreSQL execution BLOCKED pending explicit governance clearance. No schema migration or real detainee data is introduced by this gate.

# MTA DETENI — P9.11 Checkpoint: Concurrency, Failure & Security

**Status:** EXECUTABLE FOUNDATION EXPANDED / NOT CERTIFIED / MIGRATION FREEZE = TRUE / PRODUCTION = NO-GO

## Added executable coverage

- `P9.11-CONC-001` — concurrent idempotency requests: exactly one execution and deterministic replay for the remaining requests;
- `P9.11-CONC-002` — concurrent outbox worker race: only one worker receives the claim;
- `P9.11-FAIL-001` — non-retryable outbox failure becomes `DEAD_LETTER` and requires the owning lease;
- `P9.11-FAIL-002` — expired worker lease can be recovered by another worker;
- `P9.11-SEC-001..005` — authentication, scope, duty, classification, and Super Admin operational-bypass denials;
- `P9.11-SEC-006` — untrusted payload fields do not participate in authorization;
- `P9.11-GOLD-001` — synthetic temporary-exit/escort/handover/duty-completion state sequence;
- `P9.11-PROV-001` — provider invocation is modeled only after an outbox event is claimable.

## Evidence boundary

These tests are deterministic dependency-free kernel tests. They do not establish PostgreSQL transactional atomicity, real provider behavior, production HTTP integration, or production storage security. Those remain explicit gates.

## Safety boundary

All fixtures are synthetic. No real detainee data, credentials, provider secrets, production database, or schema migration is used. AI remains OFF. `NOT_RUN` and `BLOCKED` are never treated as `PASS`.

## Next checkpoint

P9.12 CI quality gate: make the executable matrix machine-verifiable in CI, preserve failure diagnostics, emit a machine-readable test summary, and keep certification blocked until CI evidence is observable and all required gates pass.

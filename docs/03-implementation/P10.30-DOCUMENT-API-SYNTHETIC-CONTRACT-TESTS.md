# P10.30 — Document API Synthetic Contract Tests

Status: PREPARED.

1. unauthenticated generation → DENY
2. missing document.generate → DENY
3. wrong scope → DENY
4. insufficient classification → DENY
5. invalid lifecycle state → DENY
6. arbitrary/unapproved template → DENY
7. valid manifest → deterministic `.docx`
8. equivalent generation → identical SHA-256
9. issue without approval → DENY
10. download without grant → DENY
11. wrong actor/scope/object → DENY
12. valid grant → exactly one success
13. replay → DENY
14. revoke → DENY
15. provider before commit → prohibited

Evidence must be synthetic, sanitized and commit-attributable. No production activation.

# P10.30 — Document API Synthetic Contract Tests

Status: PREPARED.

Required scenarios:

1. unauthenticated generation → DENY;
2. authenticated without `document.generate` → DENY;
3. wrong scope → DENY;
4. insufficient classification → DENY;
5. invalid lifecycle state → DENY;
6. unapproved/arbitrary template → DENY;
7. valid template manifest → deterministic `.docx`;
8. repeated equivalent generation → identical SHA-256;
9. issue without required approval → DENY;
10. download without grant → DENY;
11. wrong actor/scope/object grant consumption → DENY;
12. valid grant consumption → exactly one success;
13. replay → DENY;
14. revoke → DENY;
15. provider/network invocation before commit → prohibited.

Evidence must be synthetic, sanitized and attributable to the tested commit. No scenario authorizes production activation.

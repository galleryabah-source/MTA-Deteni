# P10.26 — Runtime Enforcement Matrix

Status: PREPARED — synthetic verification contract.

| Endpoint class | Auth | Permission | Scope | Duty | State | SoD | Transaction | Audit | Outbox |
|---|---|---|---|---|---|---|---|---|---|
| Protected read | required | required | required | where applicable | required | n/a | no | policy-dependent | no |
| Standard write | required | required | required | where applicable | required | required | service boundary | mandatory | where downstream exists |
| Leave transition | required | required | required | required | required | required | mandatory | mandatory | mandatory when external work exists |
| Document issue | required | document.issue | required | policy | APPROVED | ISSUER SoD | mandatory | mandatory | mandatory |
| Artifact download | required | document.view | required | policy | ISSUED | n/a | atomic grant consume | mandatory | no provider in transaction |
| RBAC mutation | required | rbac.manage | admin scope | admin authority | valid version | mandatory | mandatory | mandatory | cache invalidation |

Client resource identity is untrusted; authorization is evaluated against the resolved resource before sensitive disclosure. Every protected class requires negative-test evidence. This does not authorize production activation.

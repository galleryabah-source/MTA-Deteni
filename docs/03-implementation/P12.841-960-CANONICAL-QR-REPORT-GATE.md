# P12.841–960 — Canonical Operational, QR and Reporting Integrity Gate

## P12.841–880
A canonical operational envelope binds operation, aggregate, detainee, actor, correlation and occurrence identity. Domain modules must not create divergent identity chains for one operational action.

## P12.881–920
Operational QR has explicit contexts:
- `RUDENIM_STAY` — QR valid during the detainee's stay;
- `TEMPORARY_EXIT` — QR scan bound to the specific temporary-exit record/state;
- `DEPORTATION` — separate operational context, not a temporary-exit state.

QR is an operational verification point, not a free-form data-entry channel. Scan validity is evaluated against the explicit validity window.

## P12.921–960
Reporting snapshots carry source version, source operation identities, generation time and deterministic provenance key. Duplicate source operations are blocked.

## Synthetic E2E invariant

`Detainee → Operational Operation → QR Context/Scan → Audit/Evidence → Reporting Snapshot`

Every stage must preserve detainee and correlation identity.

## Safety

Migration Freeze TRUE; AI OFF; synthetic-only repository; production access NOT AUTHORIZED; live PostgreSQL execution BLOCKED pending governance clearance. This gate introduces no schema migration and no production data.

# P10.29 — Detainee Repository Runtime Contract

Status: CONTRACT / INFERRED

This contract is the first concrete domain-to-production-schema mapping.

Observed production source:
`public.mta_detainees`

Observed columns mapped without schema mutation:
`id, code, name, nationality, status, placement, source, metadata, created_at, updated_at`.

The repository port remains infrastructure-neutral. A future implementation must use the established runtime database adapter and authorization/transaction boundaries; it must not open an ad-hoc connection.

Safety:
- no migration;
- no production write;
- no SQL execution;
- no service-role credential in client code;
- no AI;
- no bypass of RLS/API authorization.

The checkpoint label is inferred and does not claim to be present in the supplied authoritative roadmap.

# P9.6 Runtime Database Adapter Implementation

Implements the source-defined P9.6 adapter boundary without coupling the application to a PostgreSQL client. The driver is injected and must expose connect/query/close.

Production migration roles are rejected. This module performs no migration, schema mutation, production connection, or real database execution by itself.

# P9.22 — Domain Repository Mapping

P9.22 is an implementation checkpoint derived from the approved domain contracts and P9.17 repository boundary; it is not an authoritative source label.

Purpose: explicitly map domain concepts to repository and database object identities before concrete runtime implementation.

Initial domains:
- DETAINEE
- PLACEMENT
- MOVEMENT
- LEAVE
- ESCORT
- DOCUMENT

A mapping records repository name, table identity, contract version, sensitivity, and whether an approved migration is actually required.

A mapping alone never authorizes migration.

This checkpoint does not implement SQL, ORM, production credentials, or real-data mutation.

Governance:
Migration Freeze TRUE
AI_ENABLED FALSE
Synthetic-only
No production mutation

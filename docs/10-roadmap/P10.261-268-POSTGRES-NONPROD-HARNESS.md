# MTA DETENI — P10.261–P10.268 PostgreSQL Non-Production Harness

**Status:** IMPLEMENTED CONTRACT  
**Production Database:** NOT TOUCHED  
**Migration Freeze:** TRUE  
**AI:** OFF  
**Data Boundary:** SYNTHETIC / NON-PRODUCTION ONLY

## Purpose

Extend the controlled rehearsal lane from an in-memory repository to a contract for real PostgreSQL behavior without connecting to the known production Supabase project.

## Required proof cases

1. transaction commit;
2. transaction rollback;
3. idempotency replay;
4. idempotency conflict;
5. concurrent access behavior;
6. audit atomicity;
7. outbox atomicity;
8. RLS denial.

## Hard safety boundary

Execution is accepted only when:

- environment is TEST;
- migration freeze is TRUE;
- AI is FALSE;
- explicit non-production execution opt-in is TRUE;
- the target is not a production Supabase target and does not contain production markers;
- production mutation is FALSE;
- external transport is FALSE.

This PR does **not** create a database, apply migrations, connect to a database, or claim PostgreSQL execution has passed.

P10.261–P10.268 is an inferred continuation of the controlled rehearsal sequence.

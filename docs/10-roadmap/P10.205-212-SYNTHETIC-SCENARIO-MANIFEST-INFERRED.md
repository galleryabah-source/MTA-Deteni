# MTA DETENI — P10.205–P10.212 Synthetic End-to-End Scenario Manifest

**Status:** IMPLEMENTED CONTRACT — INFERRED CONTINUATION  
**Certification:** NOT CERTIFIED  
**Migration Freeze:** TRUE  
**AI:** OFF  
**Data Boundary:** SYNTHETIC ONLY

## Basis

The controlled P10 lane identifies a non-executing scenario manifest as the next boundary after P10.189–P10.196. The exact P10.205–P10.212 labels are not source-confirmed and are therefore explicitly inferred.

## Golden-path scenario

`CREATE_DETAINEE → ASSIGN_PLACEMENT → RECORD_MOVEMENT → CREATE_LEAVE → APPROVE_LEAVE → GENERATE_DOCUMENT → ASSIGN_ESCORT → DEPART → RETURN → COMPLETE`

The manifest binds every step to one synthetic subject and one request/correlation identity.

## Required invariants

- synthetic-only;
- Migration Freeze remains active;
- AI remains OFF;
- every step has unique identity;
- step order is deterministic;
- all steps reference the same subject;
- audit is mandatory;
- outbox is mandatory;
- external transport is false;
- durable publication is false;
- production mutation is false.

## Architectural role

This is a **scenario definition and evidence boundary**, not an execution engine. It deliberately does not call repositories, PostgreSQL, Supabase, Cloudflare, Storage, queues, WhatsApp, email, AI providers, or production APIs.

## Next controlled boundary

The manifest can now be bound to the already-created P10.197–204 readiness artifact and transformed into a deterministic evidence packet for a future controlled non-production rehearsal.

# P9.8 Runtime Outbox Boundary

Defines deterministic outbox event creation and state transitions. It does not create a database table, run migrations, or dispatch external messages.

The outbox remains coupled to the transaction boundary by contract and must be persisted atomically before any external delivery is considered successful.

# P10.8 — Provenance Contract (INFERRED)
The source explicitly requires provenance under D3 and source/provenance handling, but does not explicitly number P10.8. Numbering is inferred.
Scope: provenance identity, source type/reference, capture timestamp, verification state.
No migration, persistence, real data, or mutation. Provenance must preserve source lineage rather than overwrite it.
Migration Freeze TRUE; AI_ENABLED FALSE.
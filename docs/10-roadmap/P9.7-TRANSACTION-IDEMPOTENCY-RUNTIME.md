# P9.7 Runtime Transaction + Idempotency

Adds an adapter-backed transaction runner and an in-memory idempotency store for the runtime contract.

The store is deliberately not a production persistence mechanism. No schema or migration is introduced, and no production database is accessed.

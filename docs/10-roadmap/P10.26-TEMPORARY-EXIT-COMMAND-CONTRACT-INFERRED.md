# P10.26 (Inferred) — Temporary Exit Command Contract

This checkpoint number is inferred; the authoritative source does not explicitly label P10.26.

Purpose: unify the existing Leave, Document, Escort, and Movement/Return workflow into one command validation boundary before real persistence.

Stages:
REQUEST → VALIDATE → AUTHORIZE → DOCUMENT → ESCORT → DEPART → RETURN → COMPLETE

The command must satisfy authentication, stage-specific permission, active duty, scope, policy, and current-state requirements.

Critical departure invariant:
LEAVE = APPROVED
DOCUMENT = ISSUED
ESCORT = ASSIGNED

RETURN requires LEAVE = DEPARTED.
COMPLETE requires LEAVE = RECEIVED.

This is a validation contract only. It does not mutate the database, issue documents, assign escorts, move detainees, send notifications, or invoke AI.

Governance:
Migration Freeze TRUE
AI_ENABLED FALSE
Synthetic-only
No production mutation

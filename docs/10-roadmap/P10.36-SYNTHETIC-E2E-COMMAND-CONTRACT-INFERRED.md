# P10.36 — Synthetic End-to-End Command Contract

Status: CONTRACT / INFERRED

This is the first synthetic-only end-to-end gate spanning the application's critical execution prerequisites.

The contract requires:
- synthetic-only data;
- authorization;
- idempotency ACQUIRED;
- valid domain state;
- runtime readiness;
- mandatory audit;
- required outbox availability.

Replay and conflict are never treated as executable new mutations.

This is a test/execution-readiness contract, not a production runner. It performs no database mutation and cannot authorize real detainee data.

The checkpoint label is inferred.

# P10.365–372 — Deployment Reconciliation Gate

Requires independently observed deployment evidence and runtime evidence to reference the same release commit and artifact/runtime fingerprints.

This prevents a healthy runtime observation from being attributed to a different release. It does not perform or authorize deployment.

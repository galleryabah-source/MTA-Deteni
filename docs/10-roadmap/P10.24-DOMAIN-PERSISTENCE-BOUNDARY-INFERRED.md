# P10.24 (Inferred) — Domain Persistence Boundary

P10.24 is inferred; the authoritative source does not explicitly label this checkpoint.

Purpose: define the boundary that future repositories must cross before a domain mutation can touch persistence.

Every mutation context must carry:
- transactionId;
- requestId;
- correlationId;
- actorId;
- policyVersion.

Repositories are exposed through an application boundary rather than allowing domain modules to directly construct database connections.

This contract does not install a driver, create a table, run a migration, open a production connection, or mutate data.

It prepares the next implementation gate while preserving the source-defined P9.6 database adapter and P9.7 transaction contract.

Governance:
Migration Freeze TRUE
AI_ENABLED FALSE
Synthetic-only
No production mutation

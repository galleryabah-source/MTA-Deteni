# P10.32 — Leave Repository Runtime Contract

Status: CONTRACT / INFERRED

Maps temporary-exit/leave persistence to the observed production table `public.mta_leaves`.

Observed fields:
`id, detainee_id, destination, purpose, start_at, status, metadata, created_at, updated_at`.

The repository must remain subordinate to the leave workflow, authorization kernel, transaction/idempotency boundary, mandatory audit, and outbox rules.

No schema migration, production mutation, direct SQL execution, external transport, or AI operation is performed.

The checkpoint label is inferred.

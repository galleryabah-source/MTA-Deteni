# P10.21 (Inferred) — Critical Command Gate

P10.21 is an inferred sequential checkpoint; the source does not explicitly label this number.

Purpose: make the critical mutation sequence explicit before persistence is introduced:

Authorization → Idempotency → Current State → Mandatory Audit → Transaction/Outbox boundary.

Rules:
- unauthorized commands never execute;
- idempotency CONFLICT never executes;
- REPLAY produces no new mutation;
- invalid domain state is rejected;
- mandatory audit failure fails safe;
- outbox integration remains an explicit boundary.

This contract does not perform a transaction, write audit, dispatch outbox, or mutate production data.

It operationalizes the source rule that critical mutation requiring mandatory audit must fail safely when audit evidence cannot be guaranteed.

Governance: Migration Freeze TRUE; AI_ENABLED FALSE; synthetic-only.

# P10.6 — Document Registry Domain Contract (INFERRED)

**Status: inferred sequential checkpoint — NOT source-confirmed numbering.**

## Source evidence

The authoritative source explicitly lists P10.1 DETAINEE, P10.2 PLACEMENT, P10.3 MOVEMENT, P10.4 LEAVE, followed by an ellipsis. It does not explicitly label P10.6.

It does explicitly require:
- document registry under D3 Core Administration;
- document.view and document.generate/issue authorization;
- controlled document/template versioning and historical binding;
- SHA-256 integrity and download audit in D5.

This checkpoint therefore establishes only a conservative document registry contract.

## Scope

- document identity/type;
- subject reference;
- template version;
- lifecycle status;
- content hash;
- generation timestamp;
- verification state;
- register/issue/void commands;
- mandatory void reason.

## Non-scope

No DOCX rendering, template storage, database migration, persistence, production document issuance, download, authorization bypass, AI/OCR, WhatsApp, or production mutation.

Historical documents must remain bound to their template version. Actual issue/void operations must later pass authorization, workflow/state, audit, transaction/idempotency, and applicable document controls.

Migration Freeze TRUE. AI_ENABLED FALSE.

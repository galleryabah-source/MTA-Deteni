# P9.11 — Kernel Test Harness Contract

Status: contract implementation only.

## Source basis

The authoritative project work order places P9.11 after P9.10 Observability and before P9.12 CI.

## Scope

P9.11 defines a deterministic test-harness boundary for kernel contracts.

It classifies tests as:

- UNIT;
- CONTRACT;
- INTEGRATION;
- AUTHORIZATION;
- SECURITY.

Each test case has an explicit expected outcome, and test evidence must be deterministic.

An aggregate evidence summary is PASS only when no supplied evidence item is FAIL.

## Safety boundary

This checkpoint does not:

- execute production tests against live detainee data;
- bypass authorization;
- alter database schema;
- mutate production records;
- invoke AI;
- replace the CI system.

Migration Freeze remains TRUE and repository data remains synthetic-only.

## Evidence

Implementation:
`src/infrastructure/testing/kernel-test-harness.ts`

Contract tests:
`test/p9.11-test-harness-contract.test.ts`

No claim of test execution or CI success is made here.

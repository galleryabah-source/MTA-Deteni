# P10.31 — Movement Repository Runtime Contract

Status: CONTRACT / INFERRED

Maps the movement domain to the observed production table `public.mta_movements`.

Observed fields:
`id, detainee_id, movement_type, destination, purpose, occurred_at, metadata, created_at`.

The repository is intentionally append-oriented: movement history is evidence and must not be silently overwritten.

Implementation must pass through the established authorization, idempotency, transaction, audit, outbox, and runtime adapter boundaries.

No schema change, SQL execution, production mutation, external transport, or AI operation is performed.

The checkpoint label is inferred and is not an authoritative roadmap label.

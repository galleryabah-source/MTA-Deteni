# P9.18 — Transaction/Repository Bridge

P9.18 is an implementation checkpoint derived from P9.7 transaction/idempotency and P9.17 repository boundaries; it is not an authoritative source label.

Purpose: ensure repository operations execute inside the transaction context established by the application command pipeline.

Mandatory context:
- transactionId
- requestId
- correlationId
- actorId
- policyVersion

This checkpoint provides a bridge only. It does not implement a database driver, SQL transaction, migration, schema change, audit persistence, outbox dispatch, or production mutation.

Governance:
Migration Freeze TRUE
AI_ENABLED FALSE
Synthetic-only
No production mutation

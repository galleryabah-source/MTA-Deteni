# MTA DETENI — P10.317–P10.324 Change Control Manifest

The change-control manifest binds a proposed activation to:
- change identifier;
- exact release commit;
- artifact fingerprint;
- rollback reference;
- approver identity;
- approval reason.

It is an auditable change-control record and cannot itself grant execution authorization.

P10.317–P10.324 is an inferred continuation and is not source-confirmed numbering.

# P9.11 Unified Kernel Regression Harness

## Purpose
Compose the hardened P9.7-P9.10 contracts into one deterministic synthetic journey.

## Journey
Execution Context -> Transaction -> Idempotency -> Observability -> Private Storage.

## Invariants
- identity continuity is preserved;
- transaction success commits;
- idempotency replay does not execute again;
- telemetry is correlated and redacted;
- private storage remains deny-by-default;
- context drift fails closed.

## Governance
MIGRATION_FREEZE=TRUE. Synthetic-only. No production mutation, storage upload, schema change, migration, external transport, or AI enablement.

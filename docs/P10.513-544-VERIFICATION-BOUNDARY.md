# MTA DETENI — P10.513–P10.544 Verification Boundary

Status: implementation contract established; automated execution remains pending trustworthy CI telemetry.

## P10.513–P10.520 — CI isolation

The repository CI workflow remains the authoritative automated verification path. No CI PASS is claimed until a workflow run exposes executable step telemetry and completes typecheck plus regression/domain tests.

## P10.521–P10.528 — Application composition

`src/application/mta-workflow.ts` remains the composition boundary for the canonical temporary-exit chain. UI/API layers must not bypass application/domain services. Composition must preserve authorization, validation, transaction, idempotency, audit, and outbox boundaries.

## P10.529–P10.536 — Synthetic persistence

Deterministic in-memory adapters are the current non-production persistence reference. Contract coverage includes:
- optimistic concurrency and stale-write rejection;
- append-only event duplicate rejection;
- outbox enqueue duplicate rejection;
- outbox claim and acknowledgement semantics.

These tests are synthetic only and do not imply PostgreSQL compatibility certification.

## P10.537–P10.544 — Runtime adapter boundary

The runtime contract now defines:
- explicit development/test/staging/production environment values;
- readiness evaluation over named dependencies;
- fail-closed readiness when a dependency is unavailable;
- production migration-freeze enforcement;
- runtime version requirement.

AI remains OFF by governance default. Migration remains frozen.

## Gate rule

No P10.545+ PostgreSQL adapter or schema migration is authorized by this document. A non-production PostgreSQL target requires a separate governance clearance, approved target, schema/RLS reconciliation, and evidence gate first.

# MTA DETENI — Next Gate

**Foundation:** v1.60+
**Current:** P13.5963–6040 — adapter-level synthetic runtime journey / observation pending

## Acceptance targets

1. persistent queue adapter preserves pending/synced identity semantics;
2. LAN session identity cannot be silently substituted across devices or network scopes;
3. local-PC adapter fails closed unless the service boundary and device identity are valid;
4. backup manifests form an identity-linked recovery chain;
5. offline → reconnect → reconciliation → reporting evidence remains deterministic;
6. mutation requests retain idempotency and authorization boundaries;
7. no adapter writes to live production systems;
8. no schema migration, AI activation or real detainee data.

## Following

After this gate, controlled runtime observation can validate the actual browser/LAN/local-PC stack once the GitHub runner exposes usable job telemetry and an approved non-production runtime is available.

## Governance lock

Migration Freeze TRUE. AI OFF. Repository SYNTHETIC ONLY. Production access NOT AUTHORIZED. Live PostgreSQL execution BLOCKED pending explicit governance clearance and approved non-production target.

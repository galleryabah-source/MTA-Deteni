export function createObservabilityEvent(input:any){return Object.freeze({timestamp:new Date().toISOString(),level:input.level,service:input.service,event:input.event,requestId:input.requestId,correlationId:input.correlationId,...(input.method?{method:input.method}:{}),...(input.route?{route:input.route}:{}),...(input.status!==undefined?{status:input.status}:{}),...(input.durationMs!==undefined?{durationMs:Math.max(0,input.durationMs)}:{}),...(input.outcome?{outcome:input.outcome}:{}),...(input.errorCode?{errorCode:input.errorCode}:{}),...(input.role?{role:input.role}:{}),...(input.resource?{resource:input.resource}:{})});}
export function sanitizeObservabilityError(error:unknown):string{const raw=error instanceof Error?error.message:String(error??"UNKNOWN_ERROR");return raw.replace(/Bearer\s+[A-Za-z0-9._~-]+/gi,"Bearer [REDACTED]").replace(/(?:api[_-]?key|token|secret|password)\s*[:=]\s*[^\s,;]+/gi,"$1=[REDACTED]").slice(0,240);}
export function serializeObservabilityEvent(event:any):string{return JSON.stringify(event);}

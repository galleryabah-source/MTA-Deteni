import {describe,it,expect} from 'vitest';
import {composeRuntimeEvidenceBundle} from '../src/infrastructure/certification/runtime-evidence-bundle.mjs';
const base={artifactManifestPass:true,artifactManifestFingerprint:'a'.repeat(64),runtimeHealthObserved:true,runtimeDiagnosticObserved:true,observedAt:'2026-09-21T09:40:00.000Z',health:{ok:true,app:'MTA DETENI',ai:'PROVIDER_NOT_CONFIGURED'},runtime:{mode:'PRODUCTION_RUNTIME_DIAGNOSTIC',ai:'PROVIDER_NOT_CONFIGURED'}};
describe('P10.357-364 runtime evidence bundle',()=>{it('binds live observations',()=>{const x=composeRuntimeEvidenceBundle(base);expect(x.fingerprint).toHaveLength(64);expect(x.productionMutation).toBe(false)});it('fails without health observation',()=>expect(()=>composeRuntimeEvidenceBundle({...base,runtimeHealthObserved:false})).toThrow('LIVE_HEALTH_OBSERVATION_REQUIRED'));it('fails on wrong runtime identity',()=>expect(()=>composeRuntimeEvidenceBundle({...base,runtime:{...base.runtime,mode:'OTHER'}})).toThrow('LIVE_RUNTIME_IDENTITY_INVALID'));it('fails if AI is configured',()=>expect(()=>composeRuntimeEvidenceBundle({...base,health:{...base.health,ai:'CONFIGURED'}})).toThrow('AI_STATE_INVALID'))});

import { strict as assert } from "node:assert";
import { test } from "node:test";
import { createIntegratedIntegrityAuditContinuityNode,replayIntegratedIntegrityAuditContinuityNode,certifyIntegratedIntegrityAuditContinuityNode,resetIntegratedIntegrityAuditContinuityReplayRegistry,integratedIntegrityAuditContinuityCheckpoints } from "../src/application/p13-80921-83600-integrated-integrity-audit-continuity-boundary.js";
import { integratedIntegrityAuditContinuityBCheckpoints } from "../src/application/p13-83601-86240-integrated-integrity-audit-continuity-boundary-b.js";
import { integratedIntegrityAuditContinuityCCheckpoints } from "../src/application/p13-86241-88920-integrated-integrity-audit-continuity-boundary-c.js";
import { integratedIntegrityAuditContinuityDCheckpoints } from "../src/application/p13-88921-91600-integrated-integrity-audit-continuity-boundary-d.js";
import { integratedIntegrityAuditContinuityECheckpoints } from "../src/application/p13-91601-94280-integrated-integrity-audit-continuity-boundary-e.js";
const all=[...integratedIntegrityAuditContinuityCheckpoints(),...integratedIntegrityAuditContinuityBCheckpoints(),...integratedIntegrityAuditContinuityCCheckpoints(),...integratedIntegrityAuditContinuityDCheckpoints(),...integratedIntegrityAuditContinuityECheckpoints()];
test("P13.80921-94280 registers exactly 100 unique checkpoints",()=>{assert.equal(all.length,100);assert.equal(new Set(all).size,100);assert.equal(all[0],"P13.80921-81080");assert.equal(all[99],"P13.94121-94280");});
test("continuity boundary preserves immutable review-only semantics and deterministic replay",()=>{resetIntegratedIntegrityAuditContinuityReplayRegistry();const n=createIntegratedIntegrityAuditContinuityNode({checkpoint:all[0],artifactId:"artifact-203",parentArtifactId:"artifact-202",decisionFingerprint:"fp-203",certificationArtifactId:"artifact-202",certificationDecisionFingerprint:"fp-202",auditArtifactId:"audit-202",auditDecisionFingerprint:"fp-203"});assert.equal(Object.isFrozen(n),true);assert.equal(n.state,"CONTINUITY_VERIFIED_FOR_REVIEW");assert.equal(replayIntegratedIntegrityAuditContinuityNode(n),"ADMIT");assert.equal(replayIntegratedIntegrityAuditContinuityNode(n),"REPLAY");assert.equal(replayIntegratedIntegrityAuditContinuityNode({...n,decisionFingerprint:"fp-drift"}),"CONFLICT");assert.equal(certifyIntegratedIntegrityAuditContinuityNode(n).certified,true);assert.equal(n.syntheticOnly,true);assert.equal(n.authorizationGranted,false);assert.equal(n.dispatchApproved,false);assert.equal(n.externalTransportRequested,false);assert.equal(n.dispatchExecuted,false);assert.equal(n.durablePublicationCreated,false);});
test("continuity boundary fails closed on chain mismatch",()=>{assert.throws(()=>createIntegratedIntegrityAuditContinuityNode({checkpoint:all[0],artifactId:"artifact-204",parentArtifactId:"artifact-203",decisionFingerprint:"fp-204",certificationArtifactId:"wrong-parent",certificationDecisionFingerprint:"fp-203",auditArtifactId:"audit-203",auditDecisionFingerprint:"fp-204"}),/mismatch/);});

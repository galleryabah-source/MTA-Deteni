import {describe,it,expect} from 'vitest';
import {evaluateOperationalReleaseGate} from '../src/infrastructure/certification/operational-release-gate.mjs';
const base={artifactManifestPass:true,runtimeEvidencePass:true,securityEvidencePass:true,observabilityEvidencePass:true,backupEvidencePass:true,rollbackPlanVerified:true,operatorApproval:true,changeWindowOpen:true,executionAuthorized:false,productionMutation:false,runtimeEvidence:{productionMutation:false}};
describe('P10.349-356 operational release gate',()=>{it('requires every prerequisite',()=>{const r=evaluateOperationalReleaseGate(base);expect(r.status).toBe('READY_FOR_SEPARATE_GOVERNED_PRODUCTION_DECISION');expect(r.productionCertified).toBe(false)});it('fails closed when any gate is absent',()=>{for(const k of ['artifactManifestPass','runtimeEvidencePass','securityEvidencePass','observabilityEvidencePass','backupEvidencePass','rollbackPlanVerified','operatorApproval','changeWindowOpen'])expect(()=>evaluateOperationalReleaseGate({...base,[k]:false})).toThrow('RELEASE_GATE_PREREQUISITE_FAILED:'+k)});it('never self-authorizes production',()=>{expect(()=>evaluateOperationalReleaseGate({...base,executionAuthorized:true})).toThrow('PRODUCTION_AUTHORIZATION_MUST_REMAIN_SEPARATE')})});

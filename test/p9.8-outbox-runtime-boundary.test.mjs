import {describe,it,expect} from 'vitest';import {createOutboxEvent,markOutboxDispatched,markOutboxFailed} from '../src/infrastructure/database/outbox-runtime.mjs';
const b={eventId:'E1',aggregateType:'DETAINEE',aggregateId:'D1',eventType:'DETAINEE_CREATED',payloadHash:'a'.repeat(64),occurredAt:'2026-09-21T00:00:00Z'};
describe('P9.8 outbox runtime boundary',()=>{it('creates pending event',()=>expect(createOutboxEvent(b).status).toBe('PENDING'));it('dispatches once',()=>expect(markOutboxDispatched(createOutboxEvent(b)).status).toBe('DISPATCHED'));it('retries failed event',()=>expect(markOutboxFailed(markOutboxFailed(createOutboxEvent(b))).attemptCount).toBe(2));it('rejects malformed hash',()=>expect(()=>createOutboxEvent({...b,payloadHash:'bad'})).toThrow('OUTBOX_PAYLOAD_HASH_INVALID'))});

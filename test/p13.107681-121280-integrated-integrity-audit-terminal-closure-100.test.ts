import { strict as assert } from "node:assert";
import { test } from "node:test";
import { createIntegratedIntegrityAuditTerminalClosureNode,replayIntegratedIntegrityAuditTerminalClosureNode,certifyIntegratedIntegrityAuditTerminalClosureNode,resetIntegratedIntegrityAuditTerminalClosureReplayRegistry,integratedIntegrityAuditTerminalClosureCheckpoints } from "../src/application/p13-107681-110400-integrated-integrity-audit-terminal-closure.js";
import { integratedIntegrityAuditTerminalClosureBCheckpoints } from "../src/application/p13-110401-113120-integrated-integrity-audit-terminal-closure-b.js";
import { integratedIntegrityAuditTerminalClosureCCheckpoints } from "../src/application/p13-113121-115840-integrated-integrity-audit-terminal-closure-c.js";
import { integratedIntegrityAuditTerminalClosureDCheckpoints } from "../src/application/p13-115841-118560-integrated-integrity-audit-terminal-closure-d.js";
import { integratedIntegrityAuditTerminalClosureECheckpoints } from "../src/application/p13-118561-121280-integrated-integrity-audit-terminal-closure-e.js";
const all=[...integratedIntegrityAuditTerminalClosureCheckpoints(),...integratedIntegrityAuditTerminalClosureBCheckpoints(),...integratedIntegrityAuditTerminalClosureCCheckpoints(),...integratedIntegrityAuditTerminalClosureDCheckpoints(),...integratedIntegrityAuditTerminalClosureECheckpoints()];
test("P13.107681-121280 registers exactly 100 unique checkpoints",()=>{assert.equal(all.length,100);assert.equal(new Set(all).size,100);assert.equal(all[0],"P13.107681-107840");assert.equal(all[99],"P13.121121-121280");});
test("terminal closure preserves immutable review-only semantics and deterministic replay",()=>{resetIntegratedIntegrityAuditTerminalClosureReplayRegistry();const n=createIntegratedIntegrityAuditTerminalClosureNode({checkpoint:all[0],artifactId:"artifact-204",parentArtifactId:"artifact-203",decisionFingerprint:"fp-204",closureArtifactId:"artifact-203",closureDecisionFingerprint:"fp-203",auditArtifactId:"audit-203",auditDecisionFingerprint:"fp-204"});assert.equal(Object.isFrozen(n),true);assert.equal(n.state,"TERMINAL_CLOSURE_VERIFIED_FOR_REVIEW");assert.equal(replayIntegratedIntegrityAuditTerminalClosureNode(n),"ADMIT");assert.equal(replayIntegratedIntegrityAuditTerminalClosureNode(n),"REPLAY");assert.equal(replayIntegratedIntegrityAuditTerminalClosureNode({...n,decisionFingerprint:"fp-drift"}),"CONFLICT");assert.equal(certifyIntegratedIntegrityAuditTerminalClosureNode(n).certified,true);assert.equal(n.syntheticOnly,true);assert.equal(n.authorizationGranted,false);assert.equal(n.dispatchApproved,false);assert.equal(n.externalTransportRequested,false);assert.equal(n.dispatchExecuted,false);assert.equal(n.durablePublicationCreated,false);});
test("terminal closure fails closed on continuity mismatch",()=>{assert.throws(()=>createIntegratedIntegrityAuditTerminalClosureNode({checkpoint:all[0],artifactId:"artifact-205",parentArtifactId:"artifact-204",decisionFingerprint:"fp-205",closureArtifactId:"wrong-parent",closureDecisionFingerprint:"fp-204",auditArtifactId:"audit-204",auditDecisionFingerprint:"fp-205"}),/continuity mismatch/);});

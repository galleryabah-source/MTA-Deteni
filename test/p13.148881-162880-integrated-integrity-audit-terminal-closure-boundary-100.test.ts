import { strict as assert } from "node:assert";
import { test } from "node:test";
import { createIntegratedIntegrityAuditTerminalClosureBoundary100Node,replayIntegratedIntegrityAuditTerminalClosureBoundary100Node,certifyIntegratedIntegrityAuditTerminalClosureBoundary100Node,resetIntegratedIntegrityAuditTerminalClosureBoundary100ReplayRegistry,integratedIntegrityAuditTerminalClosureBoundary100Checkpoints } from "../src/application/p13-148881-162880-integrated-integrity-audit-terminal-closure-boundary-100.js";
const all=integratedIntegrityAuditTerminalClosureBoundary100Checkpoints();
test("P13.148881-162880 registers exactly 100 unique sequential checkpoints",()=>{assert.equal(all.length,100);assert.equal(new Set(all).size,100);assert.equal(all[0],"P13.148881-149040");assert.equal(all[99],"P13.162761-162880");});
test("100-checkpoint boundary is immutable, review-only and replay deterministic",()=>{resetIntegratedIntegrityAuditTerminalClosureBoundary100ReplayRegistry();const n=createIntegratedIntegrityAuditTerminalClosureBoundary100Node({checkpoint:all[0],artifactId:"artifact-301",parentArtifactId:"artifact-300",decisionFingerprint:"fp-301",closureArtifactId:"artifact-300",closureDecisionFingerprint:"fp-300",auditArtifactId:"audit-301",auditDecisionFingerprint:"fp-301"});assert.equal(Object.isFrozen(n),true);assert.equal(n.state,"TERMINAL_CLOSURE_BOUNDARY_100_VERIFIED_FOR_REVIEW");assert.equal(replayIntegratedIntegrityAuditTerminalClosureBoundary100Node(n),"ADMIT");assert.equal(replayIntegratedIntegrityAuditTerminalClosureBoundary100Node(n),"REPLAY");assert.equal(replayIntegratedIntegrityAuditTerminalClosureBoundary100Node({...n,decisionFingerprint:"fp-drift"}),"CONFLICT");assert.equal(certifyIntegratedIntegrityAuditTerminalClosureBoundary100Node(n).certified,true);assert.equal(n.authorizationGranted,false);assert.equal(n.dispatchApproved,false);assert.equal(n.externalTransportRequested,false);assert.equal(n.dispatchExecuted,false);assert.equal(n.durablePublicationCreated,false);assert.equal(n.syntheticOnly,true);});
test("100-checkpoint boundary fails closed on continuity mismatch",()=>{assert.throws(()=>createIntegratedIntegrityAuditTerminalClosureBoundary100Node({checkpoint:all[0],artifactId:"artifact-302",parentArtifactId:"artifact-301",decisionFingerprint:"fp-302",closureArtifactId:"wrong-parent",closureDecisionFingerprint:"fp-301",auditArtifactId:"audit-302",auditDecisionFingerprint:"fp-302"}),/continuity mismatch/);});

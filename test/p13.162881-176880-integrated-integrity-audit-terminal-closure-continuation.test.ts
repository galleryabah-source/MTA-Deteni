import { strict as assert } from "node:assert";
import { test } from "node:test";
import { createIntegratedIntegrityAuditTerminalClosureContinuationNode,replayIntegratedIntegrityAuditTerminalClosureContinuationNode,certifyIntegratedIntegrityAuditTerminalClosureContinuationNode,resetIntegratedIntegrityAuditTerminalClosureContinuationReplayRegistry,integratedIntegrityAuditTerminalClosureContinuationCheckpoints } from "../src/application/p13-162881-176880-integrated-integrity-audit-terminal-closure-continuation.js";
const all=integratedIntegrityAuditTerminalClosureContinuationCheckpoints();
test("P13.162881-176880 registers exactly 100 unique sequential checkpoints",()=>{assert.equal(all.length,100);assert.equal(new Set(all).size,100);assert.equal(all[0],"P13.162881-163040");assert.equal(all[99],"P13.176741-176880");});
test("continuation preserves immutable review-only semantics and deterministic replay",()=>{resetIntegratedIntegrityAuditTerminalClosureContinuationReplayRegistry();const n=createIntegratedIntegrityAuditTerminalClosureContinuationNode({checkpoint:all[0],artifactId:"artifact-301",parentArtifactId:"artifact-300",decisionFingerprint:"fp-301",closureArtifactId:"artifact-300",closureDecisionFingerprint:"fp-300",auditArtifactId:"audit-300",auditDecisionFingerprint:"fp-301"});assert.equal(Object.isFrozen(n),true);assert.equal(n.state,"TERMINAL_CLOSURE_CONTINUATION_VERIFIED_FOR_REVIEW");assert.equal(replayIntegratedIntegrityAuditTerminalClosureContinuationNode(n),"ADMIT");assert.equal(replayIntegratedIntegrityAuditTerminalClosureContinuationNode(n),"REPLAY");assert.equal(replayIntegratedIntegrityAuditTerminalClosureContinuationNode({...n,decisionFingerprint:"fp-drift"}),"CONFLICT");assert.equal(certifyIntegratedIntegrityAuditTerminalClosureContinuationNode(n).certified,true);assert.equal(n.syntheticOnly,true);assert.equal(n.authorizationGranted,false);assert.equal(n.dispatchApproved,false);assert.equal(n.externalTransportRequested,false);assert.equal(n.dispatchExecuted,false);assert.equal(n.durablePublicationCreated,false);});
test("continuation fails closed on identity continuity mismatch",()=>{assert.throws(()=>createIntegratedIntegrityAuditTerminalClosureContinuationNode({checkpoint:all[0],artifactId:"artifact-302",parentArtifactId:"artifact-301",decisionFingerprint:"fp-302",closureArtifactId:"wrong-parent",closureDecisionFingerprint:"fp-301",auditArtifactId:"audit-301",auditDecisionFingerprint:"fp-302"}),/continuity mismatch/);});

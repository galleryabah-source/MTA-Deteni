import {describe,it,expect} from 'vitest';import {authorizePrivateObjectAccess,buildPrivateObjectMetadata} from '../src/infrastructure/storage/private-storage-runtime.mjs';
describe('P9.9 private storage runtime boundary',()=>{it('denies unauthenticated access',()=>expect(authorizePrivateObjectAccess({authenticated:false})).toEqual({allowed:false,reasonCode:'AUTH_REQUIRED'}));it('requires scope and classification',()=>expect(authorizePrivateObjectAccess({authenticated:true,scopeValid:false,classificationAllowed:true,objectId:'O1'}).reasonCode).toBe('SCOPE_DENIED'));it('builds non-public metadata',()=>expect(buildPrivateObjectMetadata({objectId:'O1',bucket:'mta-deteni-private',contentType:'application/pdf',sizeBytes:10,classification:'RESTRICTED'}).public).toBe(false));it('rejects public metadata',()=>expect(()=>buildPrivateObjectMetadata({objectId:'O1',bucket:'b',contentType:'x',sizeBytes:1,classification:'x',public:true})).toThrow('PRIVATE_STORAGE_PUBLIC_FORBIDDEN'))});

import test from "node:test";import assert from "node:assert/strict";import {runSyntheticGoldenJourney} from "../src/application/p11-synthetic-executor.ts";import {certifySyntheticRun} from "../src/application/p11-certification.ts";
test("P11.17 synthetic golden execution",()=>{const e=runSyntheticGoldenJourney({journeyId:"SYN-J1",resourceId:"DET-SYN-001",requestId:"REQ-1",correlationId:"CORR-1",actorId:"ACTOR-1",idempotencyKey:"IDEM-1"});assert.equal(e.length,9);assert.equal(e.at(-1).stage,"EVIDENCE")});
test("P11.18 certification contract pass",()=>{const e=runSyntheticGoldenJourney({journeyId:"SYN-J1",resourceId:"DET-SYN-001",requestId:"REQ-1",correlationId:"CORR-1",actorId:"ACTOR-1",idempotencyKey:"IDEM-1"});const c=certifySyntheticRun({stages:e.map(x=>x.stage),auditIds:["A1"],outboxIds:["O1"],reportId:"REP-1",replayStable:true});assert.equal(c.status,"CONTRACT_PASS")});
test("P11.19 incomplete run fails",()=>assert.equal(certifySyntheticRun({stages:["SCAN"],auditIds:["A1"],outboxIds:["O1"],reportId:"REP-1",replayStable:true}).status,"CONTRACT_FAIL"));
test("P11.20 replay instability fails",()=>assert.equal(certifySyntheticRun({stages:["SCAN","RESOLVE","DATA","ACTION","MUTATION","AUDIT","MONITOR","REPORT","EVIDENCE"],auditIds:["A1"],outboxIds:["O1"],reportId:"REP-1",replayStable:false}).status,"CONTRACT_FAIL"));

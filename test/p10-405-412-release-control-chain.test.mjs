import {describe,it,expect} from 'vitest';import {composeReleaseControlChain} from '../src/infrastructure/certification/release-control-chain.mjs';
const base=Object.fromEntries(['releaseCandidate','artifactIntegrity','runtimeVerification','operationalRelease','runtimeEvidence','deploymentReconciliation','finalReleaseEvidence','releaseVerification','productionHandoff','rollbackReadiness'].map(k=>[k,true]));
Object.assign(base,{releaseCommit:'a'.repeat(40),artifactManifestFingerprint:'b'.repeat(64),runtimeEvidenceFingerprint:'c'.repeat(64),rollbackReference:'rollback/001'});
describe('P10.405-412 release control chain',()=>{it('composes the complete chain',()=>{const x=composeReleaseControlChain(base);expect(x.chainFingerprint).toHaveLength(64);expect(x.gates).toHaveLength(10);expect(x.executionAuthorized).toBe(false)});it('fails closed on any gate',()=>expect(()=>composeReleaseControlChain({...base,rollbackReadiness:false})).toThrow('RELEASE_CONTROL_GATE_FAILED:rollbackReadiness'));it('cannot self-authorize',()=>expect(()=>composeReleaseControlChain({...base,productionCertified:true})).not.toThrow())});

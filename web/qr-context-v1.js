(()=>{'use strict';
function verify(input){const now=Date.parse(input.now||new Date().toISOString()),issued=Date.parse(input.issuedAt),expires=input.expiresAt?Date.parse(input.expiresAt):undefined;let outcome='ACCEPTED';if(!Number.isFinite(issued)||(expires!==undefined&&!Number.isFinite(expires)))outcome='REJECTED';else if(issued>now)outcome='FUTURE';else if(expires!==undefined&&now>=expires)outcome='EXPIRED';else if(input.expectedContext==='RUDENIM_STAY'&&input.context!=='DETAINEE')outcome='CONTEXT_MISMATCH';else if(input.expectedContext==='TEMPORARY_EXIT'&&input.context!=='TEMPORARY_EXIT')outcome='CONTEXT_MISMATCH';else if(input.expectedContext==='DEPORTATION'&&input.context!=='DEPORTATION')outcome='CONTEXT_MISMATCH';else if(input.activeDetainee===false)outcome='INACTIVE';const nextAction=outcome==='ACCEPTED'?'CONTINUE':outcome==='CONTEXT_MISMATCH'?'REVIEW':'STOP';return {outcome,context:input.expectedContext,detaineeId:input.detaineeId,nextAction}}
window.MTAQrContext={verify};
})();
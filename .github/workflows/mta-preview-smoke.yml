name: MTA DETENI Non-Production Preview Smoke

on:
  workflow_dispatch:
  push:
    branches: [release-candidate-2026-09-22]
    paths:
      - 'web/**'
      - 'worker-v*.js'
      - '.github/workflows/mta-preview-smoke.yml'

permissions:
  contents: read

env:
  PREVIEW_URL: https://release-candidate-2026-09-22-mta-deteni.galleryabah.workers.dev

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Wait for preview
        shell: bash
        run: |
          set -euo pipefail
          for i in $(seq 1 24); do
            code=$(curl -L -sS -o /tmp/mta-preview.html -w '%{http_code}' --max-time 10 "$PREVIEW_URL/")
            if [ "$code" = "200" ]; then
              echo "Preview HTTP 200"
              exit 0
            fi
            echo "Attempt $i: HTTP $code; retrying..."
            sleep 5
          done
          echo "Preview did not return HTTP 200"
          exit 1

      - name: Validate login gate and security headers
        shell: bash
        run: |
          set -euo pipefail
          headers=$(curl -L -sS -D - -o /tmp/mta-preview.html --max-time 15 "$PREVIEW_URL/")
          printf '%s\n' "$headers" | grep -qi '^x-frame-options: DENY'
          printf '%s\n' "$headers" | grep -qi '^x-content-type-options: nosniff'
          printf '%s\n' "$headers" | grep -qi '^x-robots-tag: noindex, nofollow'
          grep -Fq 'mta-auth-ui.js' /tmp/mta-preview.html
          curl -L -sS --fail --max-time 15 "$PREVIEW_URL/mta-auth-ui.js" -o /tmp/mta-auth-ui.js
          grep -Fq 'Silakan login untuk masuk ke aplikasi.' /tmp/mta-auth-ui.js
          grep -Fq 'OWNER' /tmp/mta-auth-ui.js
          grep -Fq 'ADMIN' /tmp/mta-auth-ui.js
          grep -Fq 'Tambah Pengguna' /tmp/mta-auth-ui.js
          grep -Fq 'adminRegister' /tmp/mta-auth-ui.js
          if grep -Fq 'Daftar Akun' /tmp/mta-auth-ui.js; then
            echo "Public registration label unexpectedly present in preview auth UI"
            exit 1
          fi
          echo "Preview login/security smoke: PASS"

export const OBSERVABILITY_VERSION='P9.10-IMPLEMENTATION-v1';
const SENSITIVE_KEYS=new Set(['authorization','cookie','password','token','access_token','refresh_token','service_role','secret']);
export function redactValue(value){if(value===null||value===undefined)return value;if(typeof value!=='object')return value;const out=Array.isArray(value)?[]:{};for(const [k,v] of Object.entries(value))out[k.toLowerCase().includes('token')||SENSITIVE_KEYS.has(k.toLowerCase())?'[REDACTED]':(typeof v==='object'&&v!==null?redactValue(v):v);return out}
export function createObservabilityEvent(input){for(const k of ['eventName','requestId','correlationId','occurredAt'])if(typeof input?.[k]!=='string'||!input[k])throw new Error('OBSERVABILITY_FIELD_REQUIRED:'+k);return Object.freeze({version:OBSERVABILITY_VERSION,eventName:input.eventName,requestId:input.requestId,correlationId:input.correlationId,occurredAt:input.occurredAt,level:input.level||'INFO',metadata:redactValue(input.metadata||{})});}

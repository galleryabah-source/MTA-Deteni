export const P11_RECOVERY_VERSION="P11-RECOVERY-v1";
export type RecoveryCase="DB_FAILURE"|"AUDIT_FAILURE"|"OUTBOX_FAILURE"|"REPORT_FAILURE"|"REPLAY";
export function assertRecoverableFailure(input:{caseType:RecoveryCase;mutationCommitted:boolean;auditCommitted:boolean;outboxCommitted:boolean}){if(input.caseType==="DB_FAILURE"&&input.mutationCommitted)throw new Error("P11_RECOVERY_DB_COMMIT_INCONSISTENT");if(input.caseType==="AUDIT_FAILURE"&&input.mutationCommitted)throw new Error("P11_RECOVERY_AUDIT_ATOMICITY");if(input.caseType==="OUTBOX_FAILURE"&&input.mutationCommitted)throw new Error("P11_RECOVERY_OUTBOX_ATOMICITY");if(input.caseType==="REPORT_FAILURE"&&(!input.mutationCommitted||!input.auditCommitted))throw new Error("P11_RECOVERY_REPORT_PRECONDITION");if(input.caseType==="REPLAY"&&!input.mutationCommitted)return true;return true;}
export function recoveryOutcome(caseType:RecoveryCase){return caseType==="REPLAY"?"IDEMPOTENT_REPLAY":"ROLLED_BACK_OR_RETRYABLE";}
